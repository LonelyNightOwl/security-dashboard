this is running in a venv (sec-dash-env) 
so always activate it (sec-dash-env\Scripts\activate)

requirements has all the tools and libraries used
wanna install something else? 
- add that in requirements.txt
- run (pip install -r requirements.txt)
